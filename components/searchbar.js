import React from "react";

const SearchBar = () => {
  return (
    <form>
      <input
        placeholder="Search"
        title="searchBar"
        class="w-40px h-20px border-black"
      />
    </form>
  );
};

export default SearchBar;
